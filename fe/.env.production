# 프로덕션 빌드(`npm run build`) 전용 값.
# Vite는 VITE_* 를 빌드 시점에 번들에 문자열로 박아넣는다.
# → Azure App Service의 "애플리케이션 설정"에 넣어도 이미 빌드된 JS에는 반영되지 않는다.
#   반드시 빌드하는 쪽(이 파일 또는 CI env)에서 값을 줘야 한다.

# be가 올라간 Azure Web App 주소 (be.yml의 app-name: yangpa-be)
VITE_API_BASE_URL=https://yangpa-be.azurewebsites.net
